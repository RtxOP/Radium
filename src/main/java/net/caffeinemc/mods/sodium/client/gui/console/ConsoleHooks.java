package net.caffeinemc.mods.sodium.client.gui.console;

public class ConsoleHooks {
    public static void render(long now) {
        // Stub: real impl draws the in-game command console on top of the HUD.
    }
}
